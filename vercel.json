{
  "version": 2,
  "routes": [
    // 🔑 API 路由：必须放在最前面！
    { "src": "/api/(.*)", "dest": "/api/$1" },

    // 🧪 你的心理测试页面（按需保留或添加）
    { "src": "/love/(.*)", "dest": "/love/$1" },
    { "src": "/color/(.*)", "dest": "/color/$1" },
    { "src": "/quiz/(.*)", "dest": "/quiz/$1" },
    { "src": "/personality/(.*)", "dest": "/personality/$1" },

    // 🏠 根路径（访问 https://www.psychtest.site/ 时显示首页）
    { "src": "/", "dest": "/index.html" },

    // 📁 其他静态文件兜底（比如 /about.html）
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
